export type AvataxClientOptions = {
  appName?: string;
  appVersion?: string;
  machineName?: string;
  accountId: string;
  licenseKey: string;
  environment: "sandbox" | "production";
  companyCode: string;
};
